import React from "react";
import { BlockQuote, Notes, Quote, Slide } from "spectacle";

export default (
  <Slide transition={["slide"]} bgColor="secondary" backgroundOpacity={0}>
    <BlockQuote>
      <Quote textSize={52}>Un sous-ensemble de TypeScript ?</Quote>
    </BlockQuote>

    <Notes></Notes>
  </Slide>
);
